{
  "scripts": {
    "dev": "bun run --watch src/server.tsx",
    "reinstall": "rm -rf node_modules bun.lockb && bun install"
  },
  "dependencies": {
    "hono": "^3.12.8"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
